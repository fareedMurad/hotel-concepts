/**
 * Props
 */
type ProgramItemProps = {
  id: number;
  name: string;
  type: string;
  category: string;
  description: string;
  price: number;
  weeks: number;
  sprints: number;
  img: string;
  slug: string;
};

export { ProgramItemProps };
