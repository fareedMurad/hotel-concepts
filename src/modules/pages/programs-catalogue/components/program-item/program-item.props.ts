/**
 * Props
 */
type ProgramItemProps = {
  name: string;
  type: string;
  category: string;
  description: string;
  price: number;
  weeks: number;
  sprints: number;
  img: string;
};

export { ProgramItemProps };
