import * as React from 'react';
import { TeachCardProps } from './teach-card.props';
import * as styles from './teach-card.scss';

/**
 * Renders TeachCard
 */
const TeachCard: React.FC<TeachCardProps> = ({}) => {
  return (
    <div className={styles.teachCard}>
      {/* content */}
      {/* content */}
      {/* content */}
    </div>
  );
};

export { TeachCard };
