/**
 * Props
 */
type CourseItemProps = {
  id: number;
  name: string;
  description: string;
  price: number;
  weeks: number;
  sprints: number;
  img?: string;
  slug: string;
};

export { CourseItemProps };
